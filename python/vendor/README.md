Vendored [pyshp 2.3.1](https://pypi.org/project/pyshp/) (`shapefile.py`).
Pure-Python ESRI shapefile reader/writer used by G-AID kernel ingest.
The catalog uses a matching TypeScript ESRI spec parser; sidecar names alone
are not support.
